query Note($id: String!) {
  note(id: $id) {
    text
    id
    title
    author
    tags {
      id
      name
      hue
    }
    deleted
    comments {
      id
      text
      createdAt
    }
  }
}
