mutation UpdateReviewAmount($reviewAmount: Int!) {
  updateReviewAmount(reviewAmount: $reviewAmount) {
    reviewAmount
    latestReviewDate
    streakBeginningDate
    missed
    reviewed
    streak
    id
  }
}
